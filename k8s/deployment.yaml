apiVersion: apps/v1
kind: Deployment
metadata:
  name: website-deployment
  namespace: production
  labels:
    app: website
spec:
  replicas: 2
  selector:
    matchLabels:
      app: website
  template:
    metadata:
      labels:
        app: website
    spec:
      containers:
      - name: website
        image: singhsantosh7891/website:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 80
